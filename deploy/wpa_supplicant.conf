# PatrolSystem WiFi 配置（供 wpa_supplicant 使用）
# 注意：若 network.conf 中设置了 WIFI_SSID，setup_network.sh 会在开机时按其重新生成本文件。
ctrl_interface=/var/run/wpa_supplicant
update_config=1

network={
    ssid="B410"
    psk="88888888"
    key_mgmt=WPA-PSK
    scan_ssid=1
}
